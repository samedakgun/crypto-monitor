{
  "name": "crypto-monitor-react",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "build:backend": "npm run build --prefix backend",
    "start:backend": "npm start --prefix backend"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
